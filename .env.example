# ===========================================
# n8n Enterprise Docker Compose Configuration
# ===========================================

# Database Configuration
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=your_secure_postgres_password_here

# n8n Configuration
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678

# Security (REQUIRED - Generate secure keys!)
# Generate with: openssl rand -base64 32
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here
N8N_JWT_SECRET=your_jwt_secret_here

# Enterprise License (REQUIRED for Enterprise features)
N8N_LICENSE_KEY=your_enterprise_license_key_here

# Performance Settings
N8N_PAYLOAD_SIZE_MAX=16
N8N_WORKER_REPLICAS=2

# Logging
N8N_LOG_LEVEL=info
N8N_LOG_OUTPUT=console,file

# Email Configuration (Optional)
N8N_EMAIL_MODE=smtp
N8N_SMTP_HOST=smtp.gmail.com
N8N_SMTP_PORT=587
N8N_SMTP_USER=your_email@domain.com
N8N_SMTP_PASS=your_app_password
N8N_SMTP_SENDER=your_email@domain.com

# Timezone
TIMEZONE=UTC

# Security Settings
N8N_SECURE_COOKIE=false

# ===========================================
# Production Settings
# ===========================================
# For production, make sure to:
# 1. Use HTTPS (set N8N_PROTOCOL=https)
# 2. Use a proper domain (set N8N_HOST to your domain)
# 3. Enable secure cookies (set N8N_SECURE_COOKIE=true)
# 4. Use strong passwords and encryption keys
# 5. Configure proper email settings
# 6. Set up SSL certificates with a reverse proxy